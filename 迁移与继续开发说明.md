# Fala Português 源码迁移包

导出日期：2026-09-04。
来源提交：e21871d5151829f2bfc75696907dcba7976e9674。
这是可编辑的网站源码，不是 Windows 安装程序或手机安装包。

## 在另一台电脑运行

1. 解压整个文件夹，不要只复制 app 文件夹。
2. 安装 Node.js（项目要求 >=22.13.0，建议使用兼容的 Node.js 22 LTS）及 pnpm。
3. 用 VS Code 或 Codex 打开解压后的项目文件夹。
4. 在该文件夹的终端执行：

```sh
npm install -g pnpm
pnpm install --frozen-lockfile
pnpm dev
```

首次安装依赖需要联网。启动后打开终端显示的本地网址；不要双击 TSX 文件。
构建检查：`pnpm build`。此项目使用 React、TypeScript、Vinext、Vite 和 Cloudflare/Sites 配置，不要直接当作普通静态 HTML 网站上传。
如果安装报错，请保留完整错误信息，不要直接删除锁文件或随意升级所有依赖。

## 主要文件

- app/page.tsx：等级选择、关卡、每日进度、听说读练习、动词变位、性别练习和导航。
- app/vocabulary.ts：A1–C2 词库、例句、词形填空逻辑。
- app/globals.css：整体样式、花边、配色和布局。
- app/layout.tsx：全局页面框架及元信息。
- public/：图片和图标。
- package.json、pnpm-lock.yaml：依赖和运行命令。
- vite.config.ts、.openai/hosting.json：开发与原 Sites 项目配置。
- PRD.md：早期产品说明，部分内容已过时，应以源码和本说明为准。

## 当前功能与交接状态

- 中葡界面切换、A1–C2 等级选择、分级词汇池。
- 每日五关：词汇 → 听力 → 阅读 → 口语 → 动词变位。
- 旧关可复习、后续关解锁、课次续学、近7天打卡。
- 写作模块已从可见入口和闯关中移除。
- 最新本地源码包含“动词变位每组5题、得分、结果页、额外练习”修改。此次导出没有重新发布，线上可能仍是此前状态。
- 尚未实现的明确需求：把地图下方“错题复习”入口替换成“常用时态动词变位整理”。请在另一台电脑继续完成，不要误以为已经实现。

## 学习数据与权限

学习进度使用当前浏览器的 localStorage，不在这些源码文件里。换电脑、换浏览器或更换网站域名不会自动带走原来的学习记录。本包不包含个人打卡数据或账户登录状态。
当前没有云端账号学习数据同步，没有通用 AI 语法批改接口。口语练习含浏览器朗读与自练，并非已接通的自动发音评分。
保留了原 .openai/hosting.json 的项目标识（不是密钥），方便同一账号继续管理原网站。在另一台电脑发布需要重新登录具备该网站权限的账号；请先本地修改和验证，再决定是否覆盖原网站。
本包排除了 node_modules、构建缓存、历史发布包、.git、环境密钥和发布凭证。没有携带依赖安装目录，因此不能离线直接运行。

## 建议优先检查的问题

- 动词题库目前只有6道共享题，需扩充分级题库；每组5题不代表有无限新题。
- 词形填空目前包含基于词干的匹配规则，不是完整葡语语法分析器。新增题库应使用明确的题干、答案、可接受变体和解析，避免误匹配。
- 动词答题保存的题号与已答状态需要补充刷新、退回、跨日等测试，防止重复计分。
- 若希望跨设备延续学习，应单独设计登录和云端进度存储，不应宣称现有本地记录会自动同步。

## 可以交给另一台电脑助手的指令

请接手这个现有葡语学习网站。先阅读迁移说明、package.json、app/page.tsx、app/vocabulary.ts，不要从零重建或丢失已有功能。先安装依赖并在本地运行，保留米白、蓝色花边和黄绿色的现有视觉风格。接着将地图下方“错题复习”替换为常用时态动词变位速查模块，按时态、动词和主语人称展示准确变位、例句与用法区别；保留每日五关和现有学习进度。验证每日5题的终止、复习、刷新恢复和计分逻辑。没有我的明确要求，不要发布覆盖线上网站。
